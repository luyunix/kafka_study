# P57 原声 ASR：Kafka的几个概念快速梳理

> 这是本地语音识别的逐段原始结果，用于核对老师是否讲过某个细节。
> 同音字、英文术语和断句可能有误；概念与命令以同目录主笔记的校正版为准。

- `00:00–00:05` 下面我们看一下怎么读取之前发生的消息。
- `00:05–00:11` 在解决这个问题之前,我们先来看一下几个概念。
- `00:11–00:13` 我们看一下课件。
- `00:13–00:18` 我们要介绍一下这么几个概念,我们把它打开看一下。
- `00:18–00:25` 这里总共有五个概念,这五个,我们分别看一下。
- `00:25–00:29` 首先前面这三个都比较好理解。
- `00:29–00:33` 这三个我们就看这张图就可以了。
- `00:33–00:39` 第一个生产者就是我们的生产者,可以有一个也可以有多个。
- `00:39–00:44` 生产者主要就是发送消息的,或者说叫写入事件。
- `00:44–00:51` 事件就是我们的数据、我们的消息,把数据写入到我们的托皮克。
- `00:51–00:58` 托皮克就是写入到我们的卡夫卡服务器,这是生产者,它主要是写入数据。
- `00:58–01:04` 它消费的扛凶码,扛凶码就右边这个是扛凶码,扛凶码也可以有一个或者多个。
- `01:04–01:09` 可以有多个扛凶码,多个消费者,这是生产者和消费者。
- `01:09–01:12` 中间这个是托皮克,这就是我们托皮克。
- `01:12–01:17` 托皮克就是在我们的卡夫卡服务器里面,我们会创建一个托皮克。
- `01:17–01:25` 我们在写入数据或是读取数据的时候,都是从托皮克去读取去写入。
- `01:25–01:29` 数据写入托皮克,读取从托皮克读取。
- `01:29–01:34` 我们在写数据、取数据都要首先创建一个托皮克。
- `01:34–01:38` 这个图就是我们这三个概念,都比较好理解。
- `01:38–01:40` 这是我们这三个概念。
- `01:40–01:44` 好,接下来我们要看右边这个图,右下角这个图。
- `01:44–01:54` 这个图就涉及到我们下面这两个概念,一个叫分区、帕滴形,一个叫偏移量、Outsight。
- `01:54–01:56` 这两个概念我们来看一下。
- `01:56–02:05` 首先我们的试试者,然后写数据、发送消息、写数据,写入到我们的托皮克里面去。
- `02:05–02:09` 托皮克就是我们卡夫卡服务器写入这里面去。
- `02:09–02:18` 我们托皮克里边可以有分区,可以有一个或者多个分区。
- `02:18–02:25` 就是一个托皮克下,可以有一个分区,或者两个分区,或者三个分区,或者是多个分区。
- `02:25–02:29` 那么分区就得扒地形,分区就得扒地形。
- `02:29–02:38` 你也可以这么的理解,比如说我们这个托皮克,我们有取个名字叫托皮克,托皮克你可以看用这个文件夹。
- `02:38–02:43` 这个文件夹下面你可以认为又有三个指文件夹,
- `02:43–02:48` 那就是我们这里面有个P1,P1,P1,P2,相予有三个指文件夹。
- `02:48–02:53` 那么这个P1,P1,P2就是那个分区,叫扒地形。
- `02:53–03:01` 就是一个托皮克下,它至少有一个分区,当然也可以有多个,默认是有一个分区。
- `03:01–03:09` 一个托皮克下,默认有一个分区,默认是一个,也可以在创建托皮克的时候,指定它有几个分区。
- `03:10–03:14` 你可以指定5个,8个,10个,指定它的分区个数。
- `03:14–03:21` 好,这就是分区概念,那就是我们这个托皮克下,托一个名字叫埋托皮克,那么它下面目前就有三个分区。
- `03:21–03:28` 当然它默认情况下,它只有一个分区,就是你不指定的话它只有一个分区,相予只有P1这一个分区。
- `03:28–03:31` 好,这就是分区的概念。
- `03:31–03:37` 好,接下来我们介绍一下这个叫偏易定,Osyde这个概念。
- `03:37–03:48` Osyde是什么呢?Osyde就是每个分区里面,你存放了消息,就是你的那个数据或者叫事件,你写入的时候就是往这个分区里面写入。
- `03:48–04:01` 比如说这是一条数据,是吧,每个格子是一个数据,每个格子是这个数据,那就是你这个每个数据的相遇它给你一个什么,有一个序号,或者说有个下标,有个序号。
- `04:02–04:10` 就是说你这个数据放了什么位置的,这个序号从零开始一直往后增长的,税续增长的,是有税续的。
- `04:10–04:18` 零一二三十五六七八九十,后面是省略的,是绝的,一直往后增长到一千一百一万这样增长。
- `04:18–04:27` 从零开始,那么你第一个消息放进来相遇这个位置就能,第一个消息放进来就是一,第三个放进来就是二,有个序号。
- `04:27–04:39` 好,这就是我们偏一亮。好,这是我们的偏一亮这个概念,就是每个分区里面,你的消息是有顺序的,是有顺序的,那么有个序号,标示它的位置。
- `04:39–04:43` 好,这就是我们这个偏一亮这个概念。
- `04:44–04:52` 那我们这个,深圳者在网里面写消息的时候,写试镜的时候,你看它是写的时候,写,写的时候它是写了什么?
- `04:52–05:00` 就写了它后面的位置,左边这个像是脑位置,后面是新位置,我们这边有个脑,欧的这个六,新的,对吧?
- `05:00–05:12` 左边是脑位置,右边是新位置,那你写的时候是要写在这个新位置上,比如说前面零到六,已经写消息了,那你地后面那个消息,那你写来到后面这个位置,写这个位置。
- `05:14–05:24` 有一个序号,好,然后我们消费的,到时候从这边去拉取消息,读取消息,那么读取消息的时候,你可以从第一个消息开始读,对吧?
- `05:24–05:31` 你也可以从最新的这个位置开始读消息,好,这是读消息。
- `05:31–05:40` 那这几个概念呢,给大家做了一个胸铃,那我们接下来看一下,在卡布卡中,每个托幣隔,就是每一个呢,这个主题是吧?
- `05:40–05:58` 它可以有一个或者多个分区,就是有一个或多个,那我们这个图里面就有三个分区,那么在创建托幣隔的时候,如果你不指定的话,那么这个托幣隔它的Party,就是它分区数量,默认呢,就是一个分区,默认就一个。
- `05:58–06:06` 你要指定三个五个,你可以指定,你不指定,它默认呢,就是一个托幣隔下,有一个分区,默认是一个。
- `06:07–06:16` 这个AuFace是标识每一个分区中消息的唯一位置,从一开始的,能一二三这样增长的。
- `06:16–06:28` 好,以上啊,就是我们对这五个概念做一个简单梳理,那下面呢,我们去写代表的时候,因为会设计到这个概念,好,那么这几个概念,小英大家呢,有一个认识的。
- `06:28–06:36` 好,那今天我们进一步来看一下,再摸去消息,再摸去消费,它之前的历史的消息。

## 机器合并全文

下面我们看一下怎么读取之前发生的消息。在解决这个问题之前,我们先来看一下几个概念。我们看一下课件。我们要介绍一下这么几个概念,我们把它打开看一下。这里总共有五个概念,这五个,我们分别看一下。首先前面这三个都比较好理解。这三个我们就看这张图就可以了。第一个生产者就是我们的生产者,可以有一个也可以有多个。生产者主要就是发送消息的,或者说叫写入事件。事件就是我们的数据、我们的消息,把数据写入到我们的托皮克。托皮克就是写入到我们的卡夫卡服务器,这是生产者,它主要是写入数据。它消费的扛凶码,扛凶码就右边这个是扛凶码,扛凶码也可以有一个或者多个。可以有多个扛凶码,多个消费者,这是生产者和消费者。中间这个是托皮克,这就是我们托皮克。托皮克就是在我们的卡夫卡服务器里面,我们会创建一个托皮克。我们在写入数据或是读取数据的时候,都是从托皮克去读取去写入。数据写入托皮克,读取从托皮克读取。我们在写数据、取数据都要首先创建一个托皮克。这个图就是我们这三个概念,都比较好理解。这是我们这三个概念。好,接下来我们要看右边这个图,右下角这个图。这个图就涉及到我们下面这两个概念,一个叫分区、帕滴形,一个叫偏移量、Outsight。这两个概念我们来看一下。首先我们的试试者,然后写数据、发送消息、写数据,写入到我们的托皮克里面去。托皮克就是我们卡夫卡服务器写入这里面去。我们托皮克里边可以有分区,可以有一个或者多个分区。就是一个托皮克下,可以有一个分区,或者两个分区,或者三个分区,或者是多个分区。那么分区就得扒地形,分区就得扒地形。你也可以这么的理解,比如说我们这个托皮克,我们有取个名字叫托皮克,托皮克你可以看用这个文件夹。这个文件夹下面你可以认为又有三个指文件夹,那就是我们这里面有个P1,P1,P1,P2,相予有三个指文件夹。那么这个P1,P1,P2就是那个分区,叫扒地形。就是一个托皮克下,它至少有一个分区,当然也可以有多个,默认是有一个分区。一个托皮克下,默认有一个分区,默认是一个,也可以在创建托皮克的时候,指定它有几个分区。你可以指定5个,8个,10个,指定它的分区个数。好,这就是分区概念,那就是我们这个托皮克下,托一个名字叫埋托皮克,那么它下面目前就有三个分区。当然它默认情况下,它只有一个分区,就是你不指定的话它只有一个分区,相予只有P1这一个分区。好,这就是分区的概念。好,接下来我们介绍一下这个叫偏易定,Osyde这个概念。Osyde是什么呢?Osyde就是每个分区里面,你存放了消息,就是你的那个数据或者叫事件,你写入的时候就是往这个分区里面写入。比如说这是一条数据,是吧,每个格子是一个数据,每个格子是这个数据,那就是你这个每个数据的相遇它给你一个什么,有一个序号,或者说有个下标,有个序号。就是说你这个数据放了什么位置的,这个序号从零开始一直往后增长的,税续增长的,是有税续的。零一二三十五六七八九十,后面是省略的,是绝的,一直往后增长到一千一百一万这样增长。从零开始,那么你第一个消息放进来相遇这个位置就能,第一个消息放进来就是一,第三个放进来就是二,有个序号。好,这就是我们偏一亮。好,这是我们的偏一亮这个概念,就是每个分区里面,你的消息是有顺序的,是有顺序的,那么有个序号,标示它的位置。好,这就是我们这个偏一亮这个概念。那我们这个,深圳者在网里面写消息的时候,写试镜的时候,你看它是写的时候,写,写的时候它是写了什么?就写了它后面的位置,左边这个像是脑位置,后面是新位置,我们这边有个脑,欧的这个六,新的,对吧?左边是脑位置,右边是新位置,那你写的时候是要写在这个新位置上,比如说前面零到六,已经写消息了,那你地后面那个消息,那你写来到后面这个位置,写这个位置。有一个序号,好,然后我们消费的,到时候从这边去拉取消息,读取消息,那么读取消息的时候,你可以从第一个消息开始读,对吧?你也可以从最新的这个位置开始读消息,好,这是读消息。那这几个概念呢,给大家做了一个胸铃,那我们接下来看一下,在卡布卡中,每个托幣隔,就是每一个呢,这个主题是吧?它可以有一个或者多个分区,就是有一个或多个,那我们这个图里面就有三个分区,那么在创建托幣隔的时候,如果你不指定的话,那么这个托幣隔它的Party,就是它分区数量,默认呢,就是一个分区,默认就一个。你要指定三个五个,你可以指定,你不指定,它默认呢,就是一个托幣隔下,有一个分区,默认是一个。这个AuFace是标识每一个分区中消息的唯一位置,从一开始的,能一二三这样增长的。好,以上啊,就是我们对这五个概念做一个简单梳理,那下面呢,我们去写代表的时候,因为会设计到这个概念,好,那么这几个概念,小英大家呢,有一个认识的。好,那今天我们进一步来看一下,再摸去消息,再摸去消费,它之前的历史的消息。
