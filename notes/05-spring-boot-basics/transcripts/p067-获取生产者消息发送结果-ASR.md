# P67 原声 ASR：获取生产者消息发送结果

> 这是本地语音识别的逐段原始结果，用于核对老师是否讲过某个细节。
> 同音字、英文术语和断句可能有误；概念与命令以同目录主笔记的校正版为准。

- `00:00–00:10` 接下来我们继续往下看一下,我们获取生产者消息发送的结果。
- `00:10–00:15` 前面我们介绍两个方法,一个是Send的方法,一个是Send的Default的方法。
- `00:15–00:20` 这两个方法都可以把我们的数据发送到Kafka里面去。
- `00:20–00:24` 这两个方法都返回comparable future这个对象。
- `00:25–00:31` 它是一个的范形的,里面是一个发送结果对象,叫SendResult对象。
- `00:31–00:35` 我们可以看一下代码,这两个方法都是返回这个对象。
- `00:35–00:39` 我们在这里打开我们这些代码,比如说我们这个Send的方法,点一下,
- `00:39–00:45` 那么它返回了comparable future这个对象,它是个范形对象。
- `00:45–00:53` 你看SendDefault的方法,点进去看一下,它也是返回一个comparable future这个对象,
- `00:53–00:56` 里面是一个SendResult结果对象。
- `00:56–01:02` 好,那这个对象是个什么对象呢?就是说,这个comparable future对象,
- `01:02–01:06` 那么这对象我们点语看一下这个原代码,点语。
- `01:06–01:12` 点语之后你发现这个代码是从GTK 1.8开始,就有这个类了。
- `01:12–01:17` 那么它是哪个类呢?它是我们加碼多线程必发编程下的一个类,
- `01:17–01:22` 加碼UTO counter这个包,必发编程包的一个类。
- `01:22–01:30` 好,这是我们这个comparable future,然后呢,它的这个里面的这个叫什么,叫SendResult。
- `01:30–01:35` 那么这是个什么类呢?这是一个棚类,它里面有两个字段,
- `01:35–01:42` 一个就是我们生产者那个记录对象,一个就是那个记录的这个元数据信息对象。
- `01:42–01:48` 这个类里面有这么两个成业变量,然后下面就是它的一个固责方法,
- `01:48–01:54` 然后get它这里面的成业变量,下面就是图示据方法,所以这个类很简单,
- `01:54–01:59` 这个类就是封装的发送后的那个结果,我这个消息发到卡不卡之后,
- `01:59–02:04` 那么它应该给我有个结果,那么这个结果呢,通过这个类的封装。
- `02:04–02:10` 好,那么这个结果用这个类的封装,这个类的这个结果信息呢,
- `02:10–02:16` 它又是放到我们这个comparable future里面的,通过它去拿你这个结果,
- `02:16–02:20` 那这个comparable future它是将要避发编程里每个类,
- `02:20–02:27` 这个类是从GTK8就1.8引入那个类,它由于异步编程,
- `02:27–02:34` 它表示的是一个异步计算的结果,comparable就表示什么意思啊,
- `02:34–02:42` 可以完成的,可完成的,对吧,future是未来的,未来完成的结果,
- `02:43–02:51` 它是一个异步啊,就是未来会完成的结果,所以现在没有结果,未来会完成的结果,将来的结果。
- `02:51–03:00` 那因为有了这样一个对象啊,它就使得我们调用者不需要等待操作完成,
- `03:00–03:06` 就能继续执行其他任务,从而提高业务程序的小异速度和吞吐量。
- `03:06–03:15` 什么意思啊,就是我去发消息,往这个卡布卡去发消息,我这个代码在那里执行,执行我发送就可以了,我不用等待。
- `03:15–03:25` 相当于我这个主线程是吧,继续往那执行,我继续往那执行,在这个位置发消息的时候就相当于开启一个新线程,
- `03:25–03:35` 让这个新线程去发消息,那我这个主线程可以继续往下执行,不用等待,在这个位置不需要等待,不需要主设。
- `03:36–03:47` 那这的话呢,我这个主线程继续往下执行,可以继续执行下面的应用代码,那那么这个发消息这里呢,它是等,它是等后续我们拿一个未来的结果,
- `03:47–03:57` 在此时刻我调这个方法去发的时候,我是不用等结果的,我不用等,我调完这个方法就直接可以继续往下执行,不用等待,不用主设。
- `03:58–04:08` 那这的话就让我们这个主线程,让我们这个调入线程可以直接往下执行,不用主设,不用等待,那么提高程序的响应速度和推动量。
- `04:09–04:19` 好,这是我们使用complete future这个类,到时候呢,到时候它这个发发消息发卡不卡,发完之后成功或者失败,
- `04:19–04:27` 到时候你可以通过这个complete future,可以拿到这个结果,它是成功还是失败,到时候用这个去拿,拿结果就可以了。
- `04:28–04:38` 好,它是这么一个情况,所以我们这个方法都是反为这个类,那下面呢,我们来去看一下这个类,怎么去拿结果。

## 机器合并全文

接下来我们继续往下看一下,我们获取生产者消息发送的结果。前面我们介绍两个方法,一个是Send的方法,一个是Send的Default的方法。这两个方法都可以把我们的数据发送到Kafka里面去。这两个方法都返回comparable future这个对象。它是一个的范形的,里面是一个发送结果对象,叫SendResult对象。我们可以看一下代码,这两个方法都是返回这个对象。我们在这里打开我们这些代码,比如说我们这个Send的方法,点一下,那么它返回了comparable future这个对象,它是个范形对象。你看SendDefault的方法,点进去看一下,它也是返回一个comparable future这个对象,里面是一个SendResult结果对象。好,那这个对象是个什么对象呢?就是说,这个comparable future对象,那么这对象我们点语看一下这个原代码,点语。点语之后你发现这个代码是从GTK 1.8开始,就有这个类了。那么它是哪个类呢?它是我们加碼多线程必发编程下的一个类,加碼UTO counter这个包,必发编程包的一个类。好,这是我们这个comparable future,然后呢,它的这个里面的这个叫什么,叫SendResult。那么这是个什么类呢?这是一个棚类,它里面有两个字段,一个就是我们生产者那个记录对象,一个就是那个记录的这个元数据信息对象。这个类里面有这么两个成业变量,然后下面就是它的一个固责方法,然后get它这里面的成业变量,下面就是图示据方法,所以这个类很简单,这个类就是封装的发送后的那个结果,我这个消息发到卡不卡之后,那么它应该给我有个结果,那么这个结果呢,通过这个类的封装。好,那么这个结果用这个类的封装,这个类的这个结果信息呢,它又是放到我们这个comparable future里面的,通过它去拿你这个结果,那这个comparable future它是将要避发编程里每个类,这个类是从GTK8就1.8引入那个类,它由于异步编程,它表示的是一个异步计算的结果,comparable就表示什么意思啊,可以完成的,可完成的,对吧,future是未来的,未来完成的结果,它是一个异步啊,就是未来会完成的结果,所以现在没有结果,未来会完成的结果,将来的结果。那因为有了这样一个对象啊,它就使得我们调用者不需要等待操作完成,就能继续执行其他任务,从而提高业务程序的小异速度和吞吐量。什么意思啊,就是我去发消息,往这个卡布卡去发消息,我这个代码在那里执行,执行我发送就可以了,我不用等待。相当于我这个主线程是吧,继续往那执行,我继续往那执行,在这个位置发消息的时候就相当于开启一个新线程,让这个新线程去发消息,那我这个主线程可以继续往下执行,不用等待,在这个位置不需要等待,不需要主设。那这的话呢,我这个主线程继续往下执行,可以继续执行下面的应用代码,那那么这个发消息这里呢,它是等,它是等后续我们拿一个未来的结果,在此时刻我调这个方法去发的时候,我是不用等结果的,我不用等,我调完这个方法就直接可以继续往下执行,不用等待,不用主设。那这的话就让我们这个主线程,让我们这个调入线程可以直接往下执行,不用主设,不用等待,那么提高程序的响应速度和推动量。好,这是我们使用complete future这个类,到时候呢,到时候它这个发发消息发卡不卡,发完之后成功或者失败,到时候你可以通过这个complete future,可以拿到这个结果,它是成功还是失败,到时候用这个去拿,拿结果就可以了。好,它是这么一个情况,所以我们这个方法都是反为这个类,那下面呢,我们来去看一下这个类,怎么去拿结果。
