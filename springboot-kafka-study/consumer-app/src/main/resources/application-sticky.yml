spring:
  kafka:
    consumer:
      properties:
        partition.assignment.strategy: org.apache.kafka.clients.consumer.StickyAssignor

app:
  kafka:
    topic: study.events.assignment
    concurrency: 3
